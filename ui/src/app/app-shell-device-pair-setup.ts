// Stub: device pair setup (mobile-only feature, stubbed for desktop builds)
import { html, type TemplateResult } from "lit";

export type DevicePairSetupHost = Record<string, unknown>;
export function renderLazyDevicePairSetup(_host: DevicePairSetupHost): TemplateResult {
  return html``;
}
