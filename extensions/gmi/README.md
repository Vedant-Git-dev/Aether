# Aether GMI Cloud Provider

Official Aether provider plugin for hosted GMI Cloud models through an
OpenAI-compatible API.

Install from Aether:

```bash
aether plugins install @aether/gmi-provider
aether gateway restart
```

Configure a GMI Cloud API key, then select models with refs such as
`gmi/openai/gpt-5.6-sol`.
