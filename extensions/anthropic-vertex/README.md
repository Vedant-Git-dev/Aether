# Aether Anthropic Vertex Provider

Official Aether provider plugin for Claude models hosted through Google Vertex AI.

Install from Aether:

```bash
aether plugins install @aether/anthropic-vertex-provider
```

Configure Google Cloud credentials and the target Vertex project/region in Aether, then select Claude models with the Anthropic Vertex provider.
