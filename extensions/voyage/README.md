# Aether Voyage Provider

Official Aether memory embedding provider plugin for Voyage AI.

Install from Aether:

```bash
aether plugins install @aether/voyage-provider
aether gateway restart
```

Set `VOYAGE_API_KEY`, then configure memory search with `provider: "voyage"`.
See <https://docs.aether.ai/reference/memory-config> for setup and
configuration.
