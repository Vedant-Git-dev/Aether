# Aether Matrix

Official Aether channel plugin for Matrix rooms and direct messages.

Install from Aether:

```bash
aether plugins install @aether/matrix
```

Configure the Matrix homeserver and bot credentials in Aether. The plugin lets agents join configured rooms, receive messages, and reply through Matrix.
