{
  "name": "@aether/geolocation-plugin",
  "version": "2026.9.4",
  "description": "Aether geolocation plugin resolving client IP addresses to a coarse city using a locally cached IP-geolocation database.",
  "repository": {
    "type": "git",
    "url": "https://github.com/aether/aether"
  },
  "type": "module",
  "dependencies": {
    "maxmind": "5.0.7"
  },
  "devDependencies": {
    "@aether/plugin-sdk": "workspace:*"
  },
  "aether": {
    "extensions": [
      "./index.ts"
    ],
    "compat": {
      "pluginApi": ">=2026.9.4"
    },
    "build": {
      "aetherVersion": "2026.9.4"
    }
  }
}
