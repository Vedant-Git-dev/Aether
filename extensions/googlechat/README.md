# Aether Google Chat

Official Aether channel plugin for Google Chat spaces and direct messages.

Install from Aether:

```bash
aether plugins install @aether/googlechat
```

Configure the Google Chat app credentials and allowed spaces in Aether. The plugin lets agents receive Google Chat events and reply through the configured app.
