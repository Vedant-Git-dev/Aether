# Aether Slack

Official Aether channel plugin for Slack channels, DMs, commands, and app events.

Install from Aether:

```bash
aether plugins install @aether/slack
```

Configure the Slack app credentials and allowed workspaces/channels in Aether. The plugin lets agents receive Slack events and reply through the configured Slack app.
