# Aether LongCat Provider

Official Aether provider plugin for LongCat-2.0 through LongCat's hosted
OpenAI-compatible API.

Install from Aether:

```bash
aether plugins install @aether/longcat-provider
aether gateway restart
```

Configure `LONGCAT_API_KEY`, then select `longcat/LongCat-2.0`.
