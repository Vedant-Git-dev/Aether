# @aether/zoom-meetings

Official Zoom browser meeting participant plugin for Aether.

This plugin registers the `zoom_meetings` tool so agents can join Zoom meetings
as a Chrome browser guest.

## Install

```bash
aether plugins install @aether/zoom-meetings
```

Restart the Gateway after installing or updating the plugin.

## Configure

Follow the Zoom meetings guide for Chrome profiles, paired nodes, audio routing,
and guest join setup:

- https://docs.aether.ai/plugins/zoom-meetings

## Package

- Plugin id: `zoom-meetings`
- Tool: `zoom_meetings`
- Package: `@aether/zoom-meetings`
- Minimum Aether host: `2026.7.2`
