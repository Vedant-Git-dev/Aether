# Distinct AETHER_* names in production source under src, packages, and extensions.
# Ratchet: lower this number when cleanup removes names; never raise it without owner approval.
# Worker bundle installation no longer injects AETHER_STATE_DIR into a remote npm process.
# The internal Node-version check no longer contributes a false AETHER_* name.
# The next-turn runtime-context preface constant no longer exists as an AETHER_* name.
492
